/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponceFeedbackResponce } from '../../models/page-responce-feedback-responce';

export interface FindFeedbackByBookId$Params {
  'book-id': number;
  page?: number;
  size?: number;
}

export function findFeedbackByBookId(http: HttpClient, rootUrl: string, params: FindFeedbackByBookId$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponceFeedbackResponce>> {
  const rb = new RequestBuilder(rootUrl, findFeedbackByBookId.PATH, 'get');
  if (params) {
    rb.path('book-id', params['book-id'], {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponceFeedbackResponce>;
    })
  );
}

findFeedbackByBookId.PATH = '/feedbacks/book/{book-id}';
